interface:
  display_name: "Dependabot"
  short_description: "Configure Dependabot updates and security automation"
  default_prompt: "Use $dependabot for this request."
policy:
  allow_implicit_invocation: true
