interface:
  display_name: "Instructor"
  short_description: "Extract structured data from LLM responses with Pydantic vali..."
  default_prompt: "Use $instructor for this request."
policy:
  allow_implicit_invocation: false
