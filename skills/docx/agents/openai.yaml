interface:
  display_name: "Docx"
  short_description: "Use this skill whenever the user wants to create, read, edit,..."
  default_prompt: "Use $docx for this request."
policy:
  allow_implicit_invocation: false
