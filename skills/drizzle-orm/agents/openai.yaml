interface:
  display_name: "Drizzle Orm"
  short_description: "Type-safe SQL ORM for TypeScript with zero runtime overhead"
  default_prompt: "Use $drizzle-orm for this request."
policy:
  allow_implicit_invocation: true
