interface:
  display_name: "Ray Train"
  short_description: "Distributed training orchestration across clusters. Scales Py..."
  default_prompt: "Use $ray-train for this request."
policy:
  allow_implicit_invocation: false
