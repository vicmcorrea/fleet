interface:
  display_name: "Rwkv Architecture"
  short_description: "RNN+Transformer hybrid with O(n) inference. Linear time, infi..."
  default_prompt: "Use $rwkv-architecture for this request."
policy:
  allow_implicit_invocation: false
