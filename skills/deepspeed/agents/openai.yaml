interface:
  display_name: "Deepspeed"
  short_description: "Expert guidance for distributed training with DeepSpeed - ZeR..."
  default_prompt: "Use $deepspeed for this request."
policy:
  allow_implicit_invocation: false
