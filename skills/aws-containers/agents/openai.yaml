interface:
  display_name: "Aws Containers"
  short_description: "Deploys and operates containerized workloads on ECS, Fargate,..."
  default_prompt: "Use $aws-containers for this request."
policy:
  allow_implicit_invocation: false
