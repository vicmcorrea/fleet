interface:
  display_name: "Long Context"
  short_description: "Extend context windows of transformer models using RoPE, YaRN..."
  default_prompt: "Use $long-context for this request."
policy:
  allow_implicit_invocation: false
