interface:
  display_name: "Ml Pipeline Workflow"
  short_description: "Build end-to-end MLOps pipelines from data preparation throug..."
  default_prompt: "Use $ml-pipeline-workflow for this request."
policy:
  allow_implicit_invocation: false
