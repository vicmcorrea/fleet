interface:
  display_name: "Model Merging"
  short_description: "Merge multiple fine-tuned models using mergekit to combine ca..."
  default_prompt: "Use $model-merging for this request."
policy:
  allow_implicit_invocation: false
