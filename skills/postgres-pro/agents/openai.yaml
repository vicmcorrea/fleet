interface:
  display_name: "Postgres Pro"
  short_description: "Use when optimizing PostgreSQL queries, configuring replicati..."
  default_prompt: "Use $postgres-pro for this request."
policy:
  allow_implicit_invocation: false
