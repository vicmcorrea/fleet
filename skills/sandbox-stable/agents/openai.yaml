interface:
  display_name: "Sandbox Stable"
  short_description: "Use when building or changing Cloudflare Sandbox apps on the..."
  default_prompt: "Use $sandbox-stable for this request."
policy:
  allow_implicit_invocation: false
