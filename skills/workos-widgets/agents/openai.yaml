interface:
  display_name: "Workos Widgets"
  short_description: "Use when the user is implementing, embedding, or debugging a..."
  default_prompt: "Use $workos-widgets for this request."
policy:
  allow_implicit_invocation: false
