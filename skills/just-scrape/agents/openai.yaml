interface:
  display_name: "Just Scrape"
  short_description: "Search, scrape, crawl, extract structured data, and monitor w..."
  default_prompt: "Use $just-scrape for this request."
policy:
  allow_implicit_invocation: false
