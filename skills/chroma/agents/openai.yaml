interface:
  display_name: "Chroma"
  short_description: "Open-source embedding database for AI applications. Store emb..."
  default_prompt: "Use $chroma for this request."
policy:
  allow_implicit_invocation: false
