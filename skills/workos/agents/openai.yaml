interface:
  display_name: "Workos"
  short_description: "Use when the user asks for a WorkOS docs URL, term, or dashbo..."
  default_prompt: "Use $workos for this request."
policy:
  allow_implicit_invocation: false
