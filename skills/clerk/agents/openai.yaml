interface:
  display_name: "Clerk"
  short_description: "Clerk authentication router. Use when user asks about adding..."
  default_prompt: "Use $clerk for this request."
policy:
  allow_implicit_invocation: true
