interface:
  display_name: "Convex Setup Auth"
  short_description: "Sets up Convex authentication with user management, identity..."
  default_prompt: "Use $convex-setup-auth for this request."
policy:
  allow_implicit_invocation: false
