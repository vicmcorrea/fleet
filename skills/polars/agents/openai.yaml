interface:
  display_name: "Polars"
  short_description: "Fast DataFrame library (Apache Arrow). Select, filter, groupb..."
  default_prompt: "Use $polars for this request."
policy:
  allow_implicit_invocation: false
