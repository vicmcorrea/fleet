interface:
  display_name: "Cli Developer"
  short_description: "Use when building CLI tools, implementing argument parsing, o..."
  default_prompt: "Use $cli-developer for this request."
policy:
  allow_implicit_invocation: false
