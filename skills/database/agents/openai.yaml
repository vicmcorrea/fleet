interface:
  display_name: "Database"
  short_description: "This skill should be used when the user wants to add a databa..."
  default_prompt: "Use $database for this request."
policy:
  allow_implicit_invocation: false
