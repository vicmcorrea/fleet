interface:
  display_name: "Workers Best Practices"
  short_description: "Reviews and authors Cloudflare Workers code against productio..."
  default_prompt: "Use $workers-best-practices for this request."
policy:
  allow_implicit_invocation: false
