interface:
  display_name: "Uv Package Manager"
  short_description: "Master the uv package manager for fast Python dependency mana..."
  default_prompt: "Use $uv-package-manager for this request."
policy:
  allow_implicit_invocation: true
