interface:
  display_name: "Axolotl"
  short_description: "Expert guidance for fine-tuning LLMs with Axolotl - YAML conf..."
  default_prompt: "Use $axolotl for this request."
policy:
  allow_implicit_invocation: false
