interface:
  display_name: "Pytorch Fsdp2"
  short_description: "Adds PyTorch FSDP2 (fullyshard) to training scripts with corr..."
  default_prompt: "Use $pytorch-fsdp2 for this request."
policy:
  allow_implicit_invocation: false
