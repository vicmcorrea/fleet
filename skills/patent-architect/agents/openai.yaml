interface:
  display_name: "Patent Architect"
  short_description: "Automatically searches prior art via SerpAPI and generates Ch..."
  default_prompt: "Use $patent-architect for this request."
policy:
  allow_implicit_invocation: false
