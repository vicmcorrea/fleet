interface:
  display_name: "Lambda Labs Gpu Cloud"
  short_description: "Reserved and on-demand GPU cloud instances for ML training an..."
  default_prompt: "Use $lambda-labs-gpu-cloud for this request."
policy:
  allow_implicit_invocation: false
