interface:
  display_name: "Cloudflare One Migrations"
  short_description: "Plans migrations from Zscaler ZIA/ZPA, Palo Alto, legacy VPN,..."
  default_prompt: "Use $cloudflare-one-migrations for this request."
policy:
  allow_implicit_invocation: false
