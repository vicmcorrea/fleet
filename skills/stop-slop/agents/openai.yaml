interface:
  display_name: "Stop Slop"
  short_description: "Remove AI writing patterns from prose. Use when drafting, edi..."
  default_prompt: "Use $stop-slop for this request."
policy:
  allow_implicit_invocation: false
