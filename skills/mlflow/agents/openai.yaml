interface:
  display_name: "Mlflow"
  short_description: "Track ML experiments, manage model registry with versioning,..."
  default_prompt: "Use $mlflow for this request."
policy:
  allow_implicit_invocation: false
