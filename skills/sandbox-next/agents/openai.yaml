interface:
  display_name: "Sandbox Next"
  short_description: "Use when building or changing Cloudflare Sandbox apps on @clo..."
  default_prompt: "Use $sandbox-next for this request."
policy:
  allow_implicit_invocation: false
