interface:
  display_name: "Cloudflare Email Service"
  short_description: "Send and receive transactional emails with Cloudflare Email S..."
  default_prompt: "Use $cloudflare-email-service for this request."
policy:
  allow_implicit_invocation: false
