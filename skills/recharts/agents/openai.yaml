interface:
  display_name: "Recharts"
  short_description: "Build composable, responsive React charts with Recharts libra..."
  default_prompt: "Use $recharts for this request."
policy:
  allow_implicit_invocation: false
