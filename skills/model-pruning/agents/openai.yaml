interface:
  display_name: "Model Pruning"
  short_description: "Prune neural networks with structured or unstructured sparsit..."
  default_prompt: "Use $model-pruning for this request."
policy:
  allow_implicit_invocation: false
