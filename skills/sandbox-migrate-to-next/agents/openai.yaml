interface:
  display_name: "Sandbox Migrate To Next"
  short_description: "Use when porting a Cloudflare Sandbox app from stable @cloudf..."
  default_prompt: "Use $sandbox-migrate-to-next for this request."
policy:
  allow_implicit_invocation: false
