interface:
  display_name: "Python Background Jobs"
  short_description: "Python background job patterns including task queues, workers..."
  default_prompt: "Use $python-background-jobs for this request."
policy:
  allow_implicit_invocation: false
