interface:
  display_name: "Runpod Deployment"
  short_description: "Deploy GPU workloads to RunPod serverless and pods - vLLM end..."
  default_prompt: "Use $runpod-deployment for this request."
policy:
  allow_implicit_invocation: false
