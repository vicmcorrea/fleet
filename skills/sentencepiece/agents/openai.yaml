interface:
  display_name: "Sentencepiece"
  short_description: "Language-independent tokenizer treating text as raw Unicode...."
  default_prompt: "Use $sentencepiece for this request."
policy:
  allow_implicit_invocation: false
