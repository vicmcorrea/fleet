interface:
  display_name: "Tensorboard"
  short_description: "Visualize training metrics, debug models with histograms, com..."
  default_prompt: "Use $tensorboard for this request."
policy:
  allow_implicit_invocation: false
