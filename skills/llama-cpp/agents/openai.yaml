interface:
  display_name: "Llama Cpp"
  short_description: "Runs LLM inference on CPU, Apple Silicon, and consumer GPUs w..."
  default_prompt: "Use $llama-cpp for this request."
policy:
  allow_implicit_invocation: false
