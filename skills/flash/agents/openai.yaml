interface:
  display_name: "Flash"
  short_description: "runpod-flash SDK and CLI for deploying AI workloads on Runpod..."
  default_prompt: "Use $flash for this request."
policy:
  allow_implicit_invocation: false
