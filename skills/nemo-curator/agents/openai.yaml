interface:
  display_name: "Nemo Curator"
  short_description: "GPU-accelerated data curation for LLM training. Supports text..."
  default_prompt: "Use $nemo-curator for this request."
policy:
  allow_implicit_invocation: false
