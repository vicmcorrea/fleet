interface:
  display_name: "Better Icons"
  short_description: "Use when working with icons in any project. Provides CLI for..."
  default_prompt: "Use $better-icons for this request."
policy:
  allow_implicit_invocation: true
