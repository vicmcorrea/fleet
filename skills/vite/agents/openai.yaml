interface:
  display_name: "Vite"
  short_description: "Vite build tool configuration, plugin API, SSR, and Vite 8 Ro..."
  default_prompt: "Use $vite for this request."
policy:
  allow_implicit_invocation: true
