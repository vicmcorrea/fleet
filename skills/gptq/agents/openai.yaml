interface:
  display_name: "Gptq"
  short_description: "Post-training 4-bit quantization for LLMs with minimal accura..."
  default_prompt: "Use $gptq for this request."
policy:
  allow_implicit_invocation: false
