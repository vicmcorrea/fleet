interface:
  display_name: "Openrouter Images"
  short_description: "Generate images from text prompts and edit existing images us..."
  default_prompt: "Use $openrouter-images for this request."
policy:
  allow_implicit_invocation: false
