interface:
  display_name: "Modal Serverless Gpu"
  short_description: "Serverless GPU cloud platform for running ML workloads. Use w..."
  default_prompt: "Use $modal-serverless-gpu for this request."
policy:
  allow_implicit_invocation: false
