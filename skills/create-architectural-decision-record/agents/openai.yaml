interface:
  display_name: "Create Architectural Decision Record"
  short_description: "Create an Architectural Decision Record (ADR) document for AI..."
  default_prompt: "Use $create-architectural-decision-record for this request."
policy:
  allow_implicit_invocation: false
