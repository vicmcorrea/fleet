interface:
  display_name: "Guidance"
  short_description: "Control LLM output with regex and grammars, guarantee valid J..."
  default_prompt: "Use $guidance for this request."
policy:
  allow_implicit_invocation: false
