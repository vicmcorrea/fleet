interface:
  display_name: "Mamba Architecture"
  short_description: "State-space model with O(n) complexity vs Transformers' O(n²)..."
  default_prompt: "Use $mamba-architecture for this request."
policy:
  allow_implicit_invocation: false
