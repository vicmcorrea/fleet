interface:
  display_name: "Dependency Upgrade"
  short_description: "Manage major dependency version upgrades with compatibility a..."
  default_prompt: "Use $dependency-upgrade for this request."
policy:
  allow_implicit_invocation: true
