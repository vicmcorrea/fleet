interface:
  display_name: "Fastapi"
  short_description: "FastAPI best practices and conventions. Use when working with..."
  default_prompt: "Use $fastapi for this request."
policy:
  allow_implicit_invocation: true
