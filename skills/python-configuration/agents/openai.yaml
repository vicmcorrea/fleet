interface:
  display_name: "Python Configuration"
  short_description: "Python configuration management via environment variables and..."
  default_prompt: "Use $python-configuration for this request."
policy:
  allow_implicit_invocation: false
