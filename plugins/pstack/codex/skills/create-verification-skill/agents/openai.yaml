interface:
  display_name: "Create Verification Skill"
  short_description: "Generate a project-local verification skill that drives your app the way a user does — any..."
  default_prompt: "Use $create-verification-skill for this request."
policy:
  allow_implicit_invocation: false
