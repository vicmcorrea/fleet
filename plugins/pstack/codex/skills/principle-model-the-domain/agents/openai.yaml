interface:
  display_name: "Principle Model The Domain"
  short_description: "Apply when writing stateful logic, or when code branches a lot or repeats a shape assumption..."
  default_prompt: "Use $principle-model-the-domain for this request."
policy:
  allow_implicit_invocation: false
