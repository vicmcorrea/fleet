interface:
  display_name: "Teach"
  short_description: "Explain a body of work plainly so a person actually understands it. Runs the `how` and `why`..."
  default_prompt: "Use $teach for this request."
policy:
  allow_implicit_invocation: false
