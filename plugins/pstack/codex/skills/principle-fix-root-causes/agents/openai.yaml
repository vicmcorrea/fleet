interface:
  display_name: "Principle Fix Root Causes"
  short_description: "Apply when debugging. Trace each symptom to its root cause and fix it there; reproduce first,..."
  default_prompt: "Use $principle-fix-root-causes for this request."
policy:
  allow_implicit_invocation: false
