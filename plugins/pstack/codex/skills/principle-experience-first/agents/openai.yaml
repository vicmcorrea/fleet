interface:
  display_name: "Principle Experience First"
  short_description: "Apply when product, UX, or feature-scope tradeoffs come up. Choose user delight over..."
  default_prompt: "Use $principle-experience-first for this request."
policy:
  allow_implicit_invocation: false
