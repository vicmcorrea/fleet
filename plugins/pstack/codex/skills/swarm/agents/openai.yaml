interface:
  display_name: "Swarm"
  short_description: "Fan out N parallel workers, drain them, and return one report. Use for /swarm, 'swarm this', or..."
  default_prompt: "Use $swarm for this request."
policy:
  allow_implicit_invocation: false
