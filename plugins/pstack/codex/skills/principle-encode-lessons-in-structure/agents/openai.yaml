interface:
  display_name: "Principle Encode Lessons In Structure"
  short_description: "Apply when you catch yourself writing the same instruction a second time, or notice a recurring..."
  default_prompt: "Use $principle-encode-lessons-in-structure for this request."
policy:
  allow_implicit_invocation: false
