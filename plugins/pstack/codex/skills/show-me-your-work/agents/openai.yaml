interface:
  display_name: "Show Me Your Work"
  short_description: "Keep a reviewable decision trail for long-running or unattended work: a TSV log with one row per..."
  default_prompt: "Use $show-me-your-work for this request."
policy:
  allow_implicit_invocation: false
