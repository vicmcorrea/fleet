interface:
  display_name: "Principle Sequence Verifiable Units"
  short_description: "Apply to multi-step work (sweeps, migrations, runs of similar edits) and to how you stack..."
  default_prompt: "Use $principle-sequence-verifiable-units for this request."
policy:
  allow_implicit_invocation: false
