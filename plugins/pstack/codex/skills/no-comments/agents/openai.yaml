interface:
  display_name: "No Comments"
  short_description: "Spawn Comment Sicko, fix accepted findings, and offer encodings for claimed constraints."
  default_prompt: "Use $no-comments for this request."
policy:
  allow_implicit_invocation: false
