interface:
  display_name: "Reflect"
  short_description: "Spawn three parallel review subagents over the active transcript, surface learnings, and route..."
  default_prompt: "Use $reflect for this request."
policy:
  allow_implicit_invocation: false
